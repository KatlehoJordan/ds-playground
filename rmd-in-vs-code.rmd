# Section 1

## Subsection 1.1

# Editing R Markdown
```{r}
linear_model <- lm(mpg ~ ., data = mtcars)
summary(linear_model)
```

```{r chunk-1}
x <- rnorm(100)
y <- rnorm(100)
```

```{r plot1}
plot(rnorm(100))
abline(h = 0, col = "red")
```

```{r plot2}
plot(rnorm(100))
abline(h = 0, col = "green")
```
