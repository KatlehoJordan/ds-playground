# Shiny apps ####

# Using `shiny` in VS Code will require additional installations including:
# - The debian tool `pandoc` (`sudo apt install pandoc -y`)

library(shiny)

shiny::runExample("01_hello")
